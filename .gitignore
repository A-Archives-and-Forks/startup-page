# dependencies
node_modules

# vscode
.vscode

# settings exports contain personal API credentials — never commit
startup-page-settings-*.json

# config files (secrets — never commit; .env.example is the tracked template)
.env
.env.dev
.env.prd
.env.development
.env.production
.env.staging

# media files
*.mp4
!desert.mp4

# macOS
.DS_STORE

# compiled
dist/
build/
.vercel
.env*.local
.env*
!.env.example
